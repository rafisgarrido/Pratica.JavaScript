function receberNomeDoMes() {
    const mapeamento = ['janeiro', 'fevereiro', 'março', 'abril', 'maio', 'junho', 'julho',
    'agosto', 'setembro', 'outubro', 'novembro', 'dezembro']
    
    for (let i = 0; i < mapeamento.length; i++){
        console.log(mapeamento[i]);
    }
    }
 console.log(receberNomeDoMes())